package `in`.hypernation.pokedex.data.remote.responses

import hypernation.pokedex.data.remote.responses.Gold
import hypernation.pokedex.data.remote.responses.Silver

data class GenerationIi(
    val crystal: Crystal,
    val gold: Gold,
    val silver: Silver
)